# Local Ollama is optional. The public app uses a grounded knowledge-base
# fallback whenever this service is unavailable.
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:7b

# Required to open the admin console in a deployed environment.
ADMIN_PASSWORD=replace-with-a-long-random-password

